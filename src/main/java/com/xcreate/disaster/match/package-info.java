package com.xcreate.disaster.match;

/**
 * 对局的运行循环。
 *
 * <p>{@link Match} 只装数据与胜负判定，不依赖服务端；{@link MatchDirector} 负责把它落到世界上——
 * 倒计时、散开、掷波、结算、关房间。分成两半是为了让「谁还算活着」「什么时候该收局」
 * 这类规则能脱离服务端单测。</p>
 *
 * <p>节奏靠一条每秒跑一次的时钟，而不是每局一串定时任务：房间数是十几的量级，扫一遍几乎不花钱；
 * 而每局一堆任务在房间被销毁时极易漏取消，留下改已卸载世界的幽灵任务。</p>
 */

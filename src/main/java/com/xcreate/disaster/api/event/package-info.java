/**
 * 对局语义事件。
 *
 * <p>这类事件是「事实层」的出口：中立地描述一局里发生了什么，不含任何平台知识。
 * 监听者拿去做录像、上报、统计都行，插件本身不为任何单一对接方让步。</p>
 *
 * <p>全部在主线程触发，且都带 {@code matchId}。监听者做 I/O 请自行异步化。</p>
 */
package com.xcreate.disaster.api.event;

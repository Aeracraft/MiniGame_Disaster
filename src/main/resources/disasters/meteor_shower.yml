id: meteor_shower
display-name: 流星雨
tier: PRIMARY
enabled: true
weight: 1.0
# 围着每个玩家散落，火流星追着人砸
spawn-strategy: AROUND_EACH_PLAYER
# 每人几个落点
point-count: 4
options:
  # 散落半径（格）
  radius: 12
  # 单个坑的半径（格），坑沿在半径处收平
  crater-radius: 3
  # 坑心往下挖几格
  crater-depth: 3
  # 站在这个距离内会被点着并掀开（格）
  scorch-radius: 6
